# Supabase
NEXT_PUBLIC_SUPABASE_URL="La tua URL del progetto Supabase"
NEXT_PUBLIC_SUPABASE_ANON_KEY="La tua chiave anon di Supabase"
SUPABASE_SERVICE_ROLE_KEY="La tua chiave service_role di Supabase (tenere segreta)"

# Stripe (se applicabile)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="La tua chiave pubblicabile di Stripe"
# STRIPE_SECRET_KEY="La tua chiave segreta di Stripe"
# STRIPE_WEBHOOK_SECRET="Il tuo segreto per il webhook di Stripe"

# Twilio (se applicabile)
# TWILIO_ACCOUNT_SID="Il tuo SID account Twilio"
# TWILIO_AUTH_TOKEN="Il tuo token di autenticazione Twilio"
# TWILIO_PHONE_NUMBER="Il tuo numero di telefono Twilio"

# URL del sito (importante per le email e i link di callback)
NEXT_PUBLIC_SITE_URL="http://localhost:3000" # Cambiare con il tuo dominio di produzione (es. https://www.moonthir.com)
